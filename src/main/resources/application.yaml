spring:
  application:
    name: papergenerator

  data:
    mongodb:
      #uri: ${MONGODB_URI}
      uri: mongodb+srv://anasaffaf_db_user:6rKiB2KWEhut6Nhb@school-app-cluster.6vnazkk.mongodb.net/  # uncomment this when running in local
      database: school_ai_db
    # Added Redis Configuration for Upstash
    redis:
      #url: ${UPSTASH_REDIS_URL}
      url: rediss://default:gQAAAAAAAdovAAIgcDFjOGI2YWZiMzcyY2Y0YmUwOTQ5OGYwMGE3NTgzZTU0Zg@flexible-dane-121391.upstash.io:6379   # uncomment this when running in local
      ssl:
        enabled: true

  ai:
    ollama:
      # Disabled auto-connection since Render won't talk to Ollama directly
      enabled: false
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.1:8b
      embedding:
        options:
          model: nomic-embed-text

    vectorstore:
      pinecone:
        #apiKey: ${PINECONE_API_KEY}
        apiKey: pcsk_4Q9qFW_Cx7sn2xMR7EjFm8H8R4KdXUS6n1SpRkGxQjpPSG7pzmd7gJxurVRfBpHKaWyhDW #uncomment this when running this on local
        index-name: "school-notes"
        project-id: "r3jj6iu"
        environment: "aped-4627-b74a"
        namespace: "default"

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB